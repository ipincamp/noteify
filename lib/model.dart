class Note {
  String title;
  String category;
  String content;
  DateTime createdAt;

  Note({
    required this.title,
    required this.category,
    required this.content,
    required this.createdAt,
  });
}
